<?xml version="1.0"?>
<project name="scala-oauth-make-jar" basedir=".">
    <property name="app.name" value="scala-oauth"/>
    <property name="app.path" value="/${app.name}"/>
    <property name="app.version" value="0.1.1"/>
    <property name="src.home" value="${basedir}/src"/>
    <property name="build.home" value="${basedir}/out"/>
    <target  name="clean">
        <delete dir="${build.home}/jar"/>
    </target>

    <target name="jar" depends="clean">
        <mkdir dir="${build.home}/jar"/>
        <jar destfile="out/jar/scala-oauth-${app.version}.jar" basedir="${build.home}/production">
            <manifest>
                <attribute name="Author" value="Ben Biddington"/>
            </manifest>
        </jar>
    </target>
</project>